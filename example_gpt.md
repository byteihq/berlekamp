Отлично — распишу сначала чистый **алгоритм Берлекемпа по шагам**, а затем подробно покажу **пошаговое вычисление на маленьком примере** (всё арифметически расчитано в $GF(5)$).

---

# Алгоритм Берлекемпа — пошагово

Вход: многочлен $f(x)\in GF(p)[x]$ степени $n$, где $p$ — простое.
Требование: $f$ — моничный и квадратсвободный (если нет — сначала делаем square-free разложение).

1. Сделать $f$ моничным и убедиться, что $\gcd(f,f')=1$ (или выполнить `squarefree_factor`).
2. Построить матрицу $Q$ размерности $n\times n$, где $j$-й столбец — коэффициенты многочлена $x^{p j} \bmod f(x)$, представленные в виде столбца коэффициентов при $1,x,x^2,\dots,x^{n-1}$.

   * Формально: $Q_{i,j} =$ коэффициент при $x^i$ в $x^{p j} \bmod f$.
3. Построить матрицу $B = Q - I$ (или, в некоторых описаниях, $B_1=(Q-I)^T$; ядро/ранг не меняются по сути).
4. Найти ядро (nullspace) матрицы $B$ над полем $GF(p)$:

   * Привести $B$ к RREF (редуцированная ступенчатая форма) — получаем опорные столбцы (pivot) и свободные столбцы.
   * Для каждой свободной столбцы $c$ можно получить базисный вектор ядра, поставив в этой свободной переменной 1, в остальных свободных — 0, и выразив опорные переменные.
   * Базис ядра — набор векторов $v^{(1)},\dots,v^{(k)}$. Каждый вектор соответствует многочлену $h_i(x)=\sum_{t=0}^{n-1} v^{(i)}_t x^t$.
   * Замечание: ядро всегда содержит вектор $[1,0,0,\dots]$ — он соответствует постоянному многочлену $1$; его обычно пропускают при поиске делителей.
5. Для каждого базисного многочлена $h(x)$ (кроме константного) перебрать все $c\in GF(p)$ и вычислить $g_c(x)=\gcd(f(x), h(x)-c)$.

   * Если для какого-то $c$ получаем нетривиальный делитель $g_c$ (то есть $1 \ne g_c \ne f$), то делим $f = g_c \cdot (f/g_c)$ и рекурсивно факторизуем каждую часть.
6. Если по всем базисным многочленам и всем $c$ не нашли нетривиального делителя — считаем $f$ неприводимым.

---

# Пример:

Возьмём $p=5$ и

$$
f(x)=x^3 + 4x + 1 \in GF(5)[x].
$$

(Это маленький пример с нетривиальным разложением: у него есть линейный множитель.)

### 1) Проверка square-free

Вычислим производную (формально):
$f'(x)=3x^2 + 4$ (коэффициенты по модулю 5).
Выполнив Евклидов алгоритм (коротко): $\gcd(f,f')=1$. Значит $f$ квадратсвободен — можно применять Берлекемпа.

### 2) Построение $Q$ (и $B=Q-I$)

Степень $n=3$. Нужно столбцы $x^{5\cdot j} \bmod f$ для $j=0,1,2$.

* $j=0$: $x^{0} \bmod f = 1$. Коэффициенты (при $1,x,x^2$): $[1,0,0]$.

* Найдём сокращения по модулю $f$. Из $f(x)=0$ имеем $x^3 \equiv -4x -1 \pmod 5$. — вычислим это в представимых числах:
  $-4 \equiv 1$ и $-1 \equiv 4$, так что $x^3 \equiv x + 4$.

* $j=1$: $x^5$.

  * $x^4 = x\cdot x^3 \equiv x(x + 4) = x^2 + 4x$.
  * $x^5 = x^2\cdot x^3 \equiv x^2(x + 4)= x^3 + 4x^2 \equiv (x+4) + 4x^2 = 4x^2 + x + 4.$
    \=> коэффициенты: $[4,1,4]$ (константа, $x$, $x^2$).

* $j=2$: $x^{10} = (x^5)^2$. Сначала умножение:
  $(4x^2 + x + 4)^2 = x^4 + 3x^3 + 3x^2 + 3x + 1$ (в промежуточных вычислениях взяты коэффициенты по модулю 5; см. ниже при необходимости детали умножения).
  Теперь подставляем $x^3 \equiv x+4$, $x^4 \equiv x^2 + 4x$:

  $$
  x^{10} \equiv (x^2+4x) + 3(x+4) + 3x^2 + 3x + 1 = 4x^2 + 0\cdot x + 3.
  $$

  \=> коэффициенты: $[3,0,4]$.

Итак, столбцы $Q$ (по $j=0,1,2$) дают матрицу (строки — коэффициенты при $1,x,x^2$):

$$
Q =
\begin{pmatrix}
1 & 4 & 3\\
0 & 1 & 0\\
0 & 4 & 4
\end{pmatrix}.
$$

Теперь $B = Q - I$:

$$
B =
\begin{pmatrix}
0 & 4 & 3\\
0 & 0 & 0\\
0 & 4 & 3
\end{pmatrix}.
$$

(в коде это именно `M` или `B` — сначала строится Q, затем вычитается единичная матрица).

### 3) Нахождение ядра $Bv=0$

Пишем систему для вектора $v=(v_0,v_1,v_2)^T$ (соответствует многочлену $v_0 + v_1 x + v_2 x^2$):

* первая строка: $0\cdot v_0 + 4 v_1 + 3 v_2 = 0$.
* вторая строка: $0=0$ (нет информации).
* третья строка: та же, $4 v_1 + 3 v_2 = 0$.

Решаем уравнение $4v_1 + 3v_2 = 0$ в $GF(5)$. Перепишем:

$$
4 v_1 = -3 v_2 \equiv 2 v_2 \pmod 5.
$$

Обратный элемент для 4 в $GF(5)$ равен 4 (потому что $4\cdot4=16\equiv1$). Умножим: $v_1 = 4\cdot 2 v_2 = 8 v_2 \equiv 3 v_2$.

Переменная $v_0$ свободна, $v_2$ свободна → размерность ядра $=2$. Можно выбрать базис:

* $e_1 = (1,0,0)$ → полином $1$ (константа).
* $e_2 = (0,3,1)$ → полином $h(x) = 3x + x^2 = x^2 + 3x$.

(метод: берем свободные переменные $v_0=1,v_2=0$ и $v_0=0,v_2=1$, выражаем $v_1$ через $v_2$).

### 4) Поиск делителя: перебираем базис $\setminus\{1\}$ и $c\in GF(5)$

Берём ненулевой базисный многочлен $h(x)=x^2+3x$. Для каждого $c\in\{0,1,2,3,4\}$ считаем $g_c(x)=\gcd(f(x), h(x)-c)$.

Нам удобно заметить: если $r$ — корень $f$ (т. е. $f(r)=0$ в поле), и если $h(r)=c$, то $x-r$ делит $h(x)-c$ и одновременно делит $f$, значит будет общий фактор. Поэтому стоило бы проверить значения $h$ в корнях $f$. У нас реальный корень найден напрямую: $f(3)=3^3+4\cdot3+1 =27+12+1=40\equiv0$ (вот почему $x=3$ — корень), поэтому $x-3$ — линейный множитель. Посчитаем $h(3)=3^2 + 3\cdot3 = 9 + 9 = 18 \equiv 3$. Значит при $c=3$ получится общий множитель, и алгоритм его найдёт.

Проверим $c=3$:

$$
h(x)-3 = x^2 + 3x -3 \equiv x^2 + 3x + 2 \pmod 5.
$$

А $x^2+3x+2 = (x+2)(x+1)$ (проверяется делением / подстановкой). Поскольку и $f$ имеет фактор $x+2$ (корень 3), то

$$
g_{3}(x)=\gcd\bigl(f(x),\,h(x)-3\bigr) = x+2
$$

(нетривиальный делитель). Алгоритм на этом шаге разделит:

$$
f(x) = (x+2)\cdot q(x),\quad q(x) = x^2 + 3x + 3
$$

(деление можно выполнить вручную — синтетическое деление даёт такой частный).

### 5) Рекурсия: факторизация частей

* Линейный множитель $x+2$ — уже неприводим.
* Квадратичный $q(x)=x^2+3x+3$. Проверим, раскладывается ли он дальше.

  * Рассчитаем дискриминант: $\Delta = b^2 - 4ac = 3^2 - 4\cdot1\cdot3 = 9 - 12 = -3 \equiv 2 \pmod 5$.
  * Квадратов в $GF(5)$ всего три: $\{0^2=0,1^2=1,2^2=4,3^2=4,4^2=1\} = \{0,1,4\}$. Число $2$ не является квадратом в $GF(5)$, значит квадратный многочлен неприводим.
  * Следовательно $q(x)$ — неприводим.

Итог:

$$
\boxed{\,f(x) = (x+2)\cdot (x^2 + 3x + 3)\,}
$$

— это полное разложение над $GF(5)$.

---

# Короткие замечания / пояснения

* **Почему ядро помогает**: базис ядра даёт многочлены $h(x)$ такие, что $h(x)^p \equiv h(x) \pmod f$. Эти $h$ конструируют разделяющие полиномы: значения $h(r)$ на корнях $r$ «стягивают» корни, и разные значения $c$ выделяют подмножества корней — через $\gcd(f,h-c)$ мы извлекаем соответствующие множители.
* **Почему пропускают константу**: константный многочлен $h(x)\equiv 1$ даёт тривиальные gcd (либо $1$, либо $f$), поэтому он бесполезен для разделения.
* **Детерминированный вариант**: в чистом (учебном) варианте перебирают все базисные $h$ и все $c\in GF(p)$. В практических реализациях иногда используют случайные комбинации базисных векторов для ускорения, но это не обязательно.
* **Проверка squarefree** обязательно перед Берлекемпом — иначе поведение матрицы и ядра будет отличаться (нужны дополнительные шаги с $p$-й степенью).

---

Отлично — делаю пункты **(2)** и **(3)** подробно и пошагово для примера
$\;p=5,\; f(x)=x^3+4x+1$ (как мы обсуждали). Покажу явное построение матрицы $Q$ и $B=Q-I$,
а затем полное приведение $B$ к RREF (всех элементарных шагов) и извлечение базиса ядра.

---

## П.2 — построение $Q$ и $B=Q-I$

Пусть $n=\deg f = 3$. По определению $Q$ — матрица $n\times n$, где $j$-й столбец это коэффициенты многочлена

$$
x^{p j}\bmod f(x)
$$

векторизованные по степеням $1,x,x^2$ (строки дают коэффициенты при $1,x,x^2$ в таком порядке).

Нам нужно $j=0,1,2$ (столбцы).

### Подготовка: редукция степеней по модулю $f$

Из $f(x)=x^3+4x+1\equiv 0$ получаем

$$
x^3 \equiv -4x -1 \pmod 5.
$$

В $\mathbb{F}_5$: $-4\equiv 1,\; -1\equiv 4$. Следовательно

$$
\boxed{\,x^3 \equiv x + 4 \pmod f. \,}
$$

Отсюда:

* $x^4 = x\cdot x^3 \equiv x(x+4)=x^2+4x.$
* $x^5 = x\cdot x^4 \equiv x(x^2+4x)=x^3+4x^2 \equiv (x+4) + 4x^2 = 4x^2 + x + 4.$

Итак для $j=1$ (столбец $x^{5}$) коэффициенты при $[1,x,x^2]$ — $[4,1,4]$.

Для $j=2$ нужен $x^{10}=(x^5)^2$. Сначала вычислим квадрат

$$
(4x^2 + x + 4)^2.
$$

Выполним раскрытие по модулю 5:

* $(4x^2)^2 = 16 x^4 \equiv 1\cdot x^4$ (так как $16\equiv1\pmod5$).
* $x^2$ даёт $x^2$.
* $4^2 = 16 \equiv 1$ — константа 1.
* Двукратные перекрестные:
  $2\cdot(4x^2\cdot x)=8 x^3 \equiv 3 x^3$ (8≡3),
  $2\cdot(4x^2\cdot 4)=32 x^2 \equiv 2 x^2$ (32≡2),
  $2\cdot(x\cdot4)=8 x \equiv 3 x$.

Собираем:

$$
x^4 + 3x^3 + (1+2)x^2 + 3x + 1 = x^4 + 3x^3 + 3x^2 + 3x + 1.
$$

Теперь подставляем $x^4$ и $x^3$:

* $x^3 \equiv x+4$ → $3x^3 \equiv 3x + 12 \equiv 3x + 2$.
* $x^4 = x\cdot x^3 \equiv x^2 + 4x$.

Итого:

$$
x^{10} \equiv (x^2+4x) + (3x+2) + 3x^2 + 3x + 1 = 4x^2 + (4x+3x+3x) + (2+1).
$$

В поле $GF(5)$: $4x+3x+3x = 10x \equiv 0$, $2+1=3$. Получаем

$$
\boxed{\,x^{10} \equiv 4x^2 + 0\cdot x + 3 \,}
$$

коэффициенты при $[1,x,x^2]$ — $[3,0,4]$.

### Собираем $Q$

* $j=0:\; x^0 \equiv 1 \Rightarrow$ столбец $[1,0,0]$.
* $j=1:\; x^5 \Rightarrow [4,1,4]$.
* $j=2:\; x^{10} \Rightarrow [3,0,4]$.

По строкам (коэффициенты при $1,x,x^2$) матрица $Q$ равна

$$
Q=\begin{pmatrix}
1 & 4 & 3\\[2pt]
0 & 1 & 0\\[2pt]
0 & 4 & 4
\end{pmatrix}.
$$

Матрица $B = Q - I$ (вычитаем единичную на диагонали):

$$
B=\begin{pmatrix}
1-1 & 4   & 3\\
0   & 1-1 & 0\\
0   & 4   & 4-1
\end{pmatrix}
=
\begin{pmatrix}
0 & 4 & 3\\
0 & 0 & 0\\
0 & 4 & 3
\end{pmatrix}.
$$

(Это и есть матрица $B$ со страницы / картинки.)

---

## П.3 — нахождение базиса ядра $\ker(B)$ (RREF пошагово)

Решаем систему $B v = 0$ для $v=(v_0,v_1,v_2)^T$ над $GF(5)$.
Начальная матрица (записываем строки):

$$
\begin{pmatrix}
0 & 4 & 3\\
0 & 0 & 0\\
0 & 4 & 3
\end{pmatrix}.
$$

Приведём её к RREF (редуцированная ступенчатая форма). Работать будем по строкам (элементарные операции над строками, арифметика по модулю 5).

**Шаг 1.** Первый столбец полностью нулевой → нет опорного элемента в первом столбце.

Ищем первый ненулевой столбец — это 2-й столбец (индекс 1), в нём строки 1 и 3 имеют по 4.

Пусть мы выберем строку 1 как первую ненулевую строку.

Матрица сейчас:

$$
R_1 = [0\;\;4\;\;3],\quad R_2 = [0\;\;0\;\;0],\quad R_3 = [0\;\;4\;\;3].
$$

**Шаг 2.** Сделаем ведущий элемент = 1. В поле $GF(5)$ обратный элемент к 4 — это 4 (т.к. $4\cdot4=16\equiv1$). Умножим $R_1$ на $4$:

$$
R_1 \leftarrow 4\cdot R_1 = [0,\;4\cdot4,\;4\cdot3] \equiv [0,\;1,\;12]\equiv[0,\;1,\;2].
$$

(12 ≡ 2 mod 5)

Теперь

$$
R_1 = [0\;1\;2],\quad R_2=[0\;0\;0],\quad R_3=[0\;4\;3].
$$

**Шаг 3.** Обнулим все остальные элементы в столбце 2 (везде, где есть не ноль) с помощью $R_1$.

* $R_3$ имеет в столбце 2 значение 4, поэтому сделаем $R_3 \leftarrow R_3 - 4\cdot R_1$.

Вычислим $4\cdot R_1 = [0,\;4,\;8]\equiv[0,4,3]$ (так как 8≡3). Тогда

$$
R_3 - 4R_1 = [0,4,3] - [0,4,3] = [0,0,0].
$$

После этого матрица:

$$
\begin{pmatrix}
0 & 1 & 2\\
0 & 0 & 0\\
0 & 0 & 0
\end{pmatrix}.
$$

Это уже RREF: есть один ведущий столбец (колонка 2), ведущая строка — первая, все остальные строки нулевые, в ведущей строке ведущая позиция равна 1 и во всех прочих строках соответствующие столбцы нули.

---

### Информация о ранге и свободных переменных

* Ранг $=$ число ненулевых строк $=1$.
* Число столбцов $=3$. Значит размерность ядра $= 3 - 1 = 2$.
* Ведущий (pivot) столбец — это второй столбец (индекс 1). Значит переменная $v_1$ выражается через остальные; свободные переменные — $v_0$ и $v_2$.

Уравнение, соответствующее ведущей строке:

$$
1\cdot v_1 + 2\cdot v_2 = 0 \quad\Longrightarrow\quad v_1 = -2 v_2 \equiv 3 v_2 \ (\text{mod }5).
$$

Переменная $v_0$ полностью свободна.

### Базис ядра

Выбираем стандартный способ получить базис: для каждой свободной переменной ставим её =1, остальные свободные =0, и вычисляем опорные переменные.

1. Возьмём $v_0=1,\; v_2=0$. Тогда $v_1 = 3\cdot 0 = 0$. Получаем

$$
v^{(1)} = (1,0,0).
$$

Это соответствует многочлену $h^{(1)}(x) = 1$ (константа).

2. Возьмём $v_0=0,\; v_2=1$. Тогда $v_1 = 3\cdot1 = 3$. Получаем

$$
v^{(2)} = (0,3,1).
$$

Это соответствует многочлену

$$
h^{(2)}(x) = 0 + 3x + 1\cdot x^2 = x^2 + 3x.
$$

Именно эти два вектора формируют базис ядра (размерность 2).

---

## Итог (что мы получили)

* Матрица $Q$ и $B$:

  $$
  Q=\begin{pmatrix}1&4&3\\0&1&0\\0&4&4\end{pmatrix},\qquad
  B=Q-I=\begin{pmatrix}0&4&3\\0&0&0\\0&4&3\end{pmatrix}.
  $$
* RREF(B) = $\begin{pmatrix}0&1&2\\0&0&0\\0&0&0\end{pmatrix}$.
* Базис ядра $\ker(B)$: векторы $(1,0,0)$ и $(0,3,1)$, соответствующие многочленам $1$ и $x^2+3x$.
* Как и ожидалось, константный многочлен $1$ полезен в ядре, но для разделения $f$ мы берём ненулевую второй базисный многочлен $h(x)=x^2+3x$ и проверяем $\gcd(f,h-c)$ по всем $c\in GF(5)$ — именно это даёт разбиение $f=(x+2)\cdot (x^2+3x+3)$, как было показано ранее.

---


